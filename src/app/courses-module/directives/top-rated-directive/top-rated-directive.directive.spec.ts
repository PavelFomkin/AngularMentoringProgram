import {TopRatedDirective} from './top-rated.directive';

describe('TopRatedDirective', () => {
  it('should create an instance', () => {
    // const directive = new TopRatedDirective();
    // expect(directive).toBeTruthy();
  });
});
